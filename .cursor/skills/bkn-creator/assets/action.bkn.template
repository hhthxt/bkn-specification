---
type: action
id: {action_id}
name: {行动名称}
version: 1.0.0
spec_version: 1.0.0
network: {network_id}
namespace: {namespace}
owner: {owner}
action_type: add | modify | delete
# 治理字段：enabled=false 默认不可执行；risk_level=high 或 requires_approval=true 需审批/dry-run
enabled: false
risk_level: low | medium | high
requires_approval: true
tags: [{标签1}, {标签2}]
---

<!-- 治理字段填写指引（生成时删除）：
  enabled: 查询/只读可 true；修改/删除/高危建议 false
  risk_level: low=只读, medium=有副作用, high=删除/生产变更
  requires_approval: high 或生产操作建议 true -->

## Action: {action_id}

{详细描述}

### Bound Object

| Bound Object | Action Type |
|--------------|-------------|
| {object_id} | modify |

### Trigger Condition

```yaml
condition:
  object_type_id: {object_id}
  field: {property_name}
  operation: {operator}
  value: {value}
```

> {触发条件说明}

### Pre-conditions

<!-- 可选：执行前的数据前置条件，不满足则阻止执行 -->

| Object | Check | Condition | Message |
|--------|-------|-----------|---------|
| {object_id} | relation:{relation_id} | exist | {违反说明} |
| {object_id} | property:{property_name} | {op} {value} | {违反说明} |

### Scope of Impact

| Impacted Object | Impact Description |
|-----------------|---------------------|
| {object_id} | {影响描述} |

### Tool Configuration

| Type | Toolbox ID | Tool ID |
|------|----------|--------|
| tool | {box_id} | {tool_id} |

<!-- 或使用 MCP -->
<!-- | 类型 | MCP ID | 工具名称 |
|------|--------|----------|
| mcp | {mcp_id} | {tool_name} | -->

### 参数绑定

| 参数 | 类型 | 来源 | 绑定 | 说明 |
|------|------|------|------|------|
| {param} | string | property | {property_name} | {说明} |
| {param} | string | input | - | {说明} |
| {param} | string | const | {value} | {说明} |

### Schedule

<!-- 可选 -->

| Type | Expression |
|------|--------|
| FIX_RATE | {interval} |

### Permissions and Prerequisites

{谁可以启用/执行、是否需要审批、需要哪些凭据/权限}

### Rollback/Failure Policy

{失败如何处理、是否重试、是否可撤销、如何回滚}

### Execution Flow

1. {步骤1}
2. {步骤2}
3. {步骤3}
